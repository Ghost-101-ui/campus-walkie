/**
 * Configuration for the Background System.
 * Options for style: "paper" | "dot" | "grid" | "notebook"
 */
export const backgroundConfig = {
  style: "paper" as "paper" | "dot" | "grid" | "notebook",
  opacity: 0.04,
};
